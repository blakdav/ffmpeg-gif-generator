# ffmpeg-gif-generator
Self-hosted Docker container for converting video files to GIF. Drop a video into the web UI, tweak FPS (1–30), output width, start time, and duration, then download your GIF. Uses FFmpeg 2-pass palette generation for better quality output.
